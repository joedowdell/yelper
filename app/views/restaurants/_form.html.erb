<%= @restaurant.errors.any? %>
	<section id="error_explanation">
		<h2><%= pluralize(@restaurant.errors.count, "errors") %> prohibited this restaurant from being saved:</h2>

		<ul>
		<% @restaurant.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		</ul>
	</section>
<% end %>

<%= form_for @restaurant do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>